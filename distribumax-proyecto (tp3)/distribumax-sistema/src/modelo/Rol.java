package modelo;

/**
 * Roles posibles de un usuario del sistema.
 * Coincide con el tipo ENUM de la columna rol en la tabla usuarios (TP2).
 */
public enum Rol {
    ADMIN,
    VENDEDOR,
    OPERADOR,
    TRANSPORTISTA
}
